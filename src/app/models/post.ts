// Definición de la interfaz Post, que describe la estructura de un objeto post

export interface Post {
    userId: number;  // Identificador del usuario que creó el post
    id: number;      // Identificador único del post
    title: string;   // Título del post
    body: string;    // Contenido o cuerpo del post
  }
  